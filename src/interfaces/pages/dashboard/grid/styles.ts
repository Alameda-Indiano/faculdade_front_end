import { SxProps } from '@mui/material';

export const styles = {
	tilesContainer: {
		width: '100%',
		display: 'flex',
		flexWrap: 'wrap',
		justifyContent: 'space-between',
		alignItems: 'center',
		mt: 10,
		mb: 10,
		pl: 40,
		pr: 40,
	},
} satisfies Record<string, SxProps>;
