import { SxProps } from '@mui/material';

export const styles = {
	tilesContainer: {
		width: '100%',
		display: 'flex',
		flexWrap: 'wrap',
		justifyContent: 'space-between',
		alignItems: 'center',
		pl: 40,
		pr: 40,
		mb:10
	},
} satisfies Record<string, SxProps>;
